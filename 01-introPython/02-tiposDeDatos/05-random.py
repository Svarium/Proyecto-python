import random

x = random.randrange(1,10) # Generates a random integer from 1 to 9
print(x)

y = random.random() # Generates a random float from 0.0 to 1.0
print(y)

z = random.randint(1, 10) # Generates a random integer from 1 to 10 (inclusive)
print(z)